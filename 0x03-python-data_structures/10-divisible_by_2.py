#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    list2 = []
    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
            list.append(my_list[i])
            return (True)
        else:
            return (False)
