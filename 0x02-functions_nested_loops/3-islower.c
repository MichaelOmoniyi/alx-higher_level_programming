inclued "main.h"

/**
 * islower - checks for lower case character
 *
 * Return: 0
 */

int _islower (int c)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch = 'c')
			return (ch);
		else
			return (0);
	}
}	
