#include <stdio.h>

/**
 * main - executes statement
 * Return: 0 when succesful
*/

int main(void)
{
	unsigned int a;

	a = 0;

	while (0 < 10)
	{
		putchar (a);
		a++;
	}
	putchar ('\n');
	return (0);
}
