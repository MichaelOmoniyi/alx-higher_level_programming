#!/usr/bin/python3

def square_matrix_simple(matrix=[]):
    new_matrix = []
    for row in matrix:
        for i in row:
            i ** 2
            return (row)
        new_matrix.append(row)
        return (new_matrix)
