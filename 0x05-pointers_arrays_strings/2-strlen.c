#include "main.h"
#include <string.h>

/**
 * _strlen - Returns the length of a string
 * @s: input character parameter
 */

int _strlen(char *s)
{
	char str;
	int len;

	*s = str;
	len = strlen(str);

	return (len);
}
