#!/usr/bin/python3

if __name__ == "__main__":
    if len(argv) = 1:
        print(f"{len(argv} argument:")
    elif len(argv) > 1:
        print(f"{len(argv} arguments:")
    else:
        print("0 arguments.")

    while count < len(argv):
        print(f"{count + 1}: {argv[count]}")
